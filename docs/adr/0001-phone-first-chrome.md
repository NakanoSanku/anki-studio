# Phone-first chrome; wide screens only tile panes

The UI is one phone information architecture at every width: no desktop sidebar, no second navigation. Above a large breakpoint, already-existing panes (for example note list beside editor) may sit side by side as progressive enhancement. Desktop authoring keeps a keyboard, not a separate product.
