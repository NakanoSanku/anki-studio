# 列目录仍物化全部卡包预览；读请求必须合并

每次云同步拉目录都会扫 spreadsheet 里每一个卡包的可读预览，把表格里改过的笔记写回本机。这是产品行为，不是可以改回「只扫当前卡包」的实现细节。

Google Sheets 按「每用户每分钟读请求」计费，逐表 `values.get` 会在十个以上卡包时打满配额。因此同一轮同步里 spreadsheet metadata 只拉一次，目录、卡包数据表和预览表用 `values.batchGet` 合并；`/api/sync/status` 不再为「是否可用」去读表格。配额 403 按频率限制处理并短回退重试，不当成无权访问。
