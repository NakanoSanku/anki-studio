# `/` is the 学习 queue; `/study` is the 会话

The PWA start URL is `/` (today's queue). An active 会话 lives at `/study`. The system back button exits `/study` to `/` and must not close the installed app. Do not auto-start a 会话 when cards are due.
